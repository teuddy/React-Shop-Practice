import React from 'react';
import PropTypes from 'prop-types';
import { Carousel } from 'react-responsive-carousel';

class CarouselImported extends React.Component {
  render() {
    return <div />;
  }
}
export default CarouselImported;

/**
 * 
 <Carousel
        showThumbs={false}
        showArrow={true}
        infiniteLoop={true}
        interval={5000}
        showStatus={false}
        autoPlay={true}
      >
 */
